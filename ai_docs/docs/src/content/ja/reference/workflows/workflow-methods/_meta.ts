const meta = {
  then: ".then()",
  branch: ".branch()",
  parallel: ".parallel()",
  commit: ".commit()",
  dowhile: ".dowhile()",
  dountil: ".dountil()",
  foreach: ".foreach()",
  map: ".map()",
  sleep: ".sleep()",
  sleepUntil: ".sleepUntil()",
  waitForEvent: ".waitForEvent()",
  sendEvent: ".sendEvent()",
  "create-run": ".createRunAsync()",
};
export default meta;
